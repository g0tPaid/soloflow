{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd ../.. && (pnpm --filter @flowbooks/database exec prisma migrate deploy || true) && cd apps/api && node dist/main",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
